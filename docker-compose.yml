# D7 Risk BFF — sin infra (reglas y persistencia in-memory).

services:
  app:
    build: .
    container_name: w2m-virtual-d7-risk-bff
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8086}:8086"
